{
  "name": "functions",
  "private": true,
  "scripts": {
    "serve": "nx run functions:build && npx firebase emulators:start --only functions",
    "shell": "nx run functions:build && npx firebase functions:shell",
    "deploy": "nx run functions:build && npx firebase deploy --only functions",
    "logs": "npx firebase functions:log"
  },
  "engines": {
    "node": ">=20"
  },
  "main": "lib/src/index.js",
  "types": "./functions/src/index.d.ts",
  "type": "commonjs"
}