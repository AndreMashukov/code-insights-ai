{
  "name": "functions",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "functions/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn build",
        "cwd": "functions"
      },
      "dependsOn": ["^build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn lint",
        "cwd": "functions"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd functions && yarn build && cd .. && npx firebase emulators:start"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn deploy",
        "cwd": "functions"
      }
    },
    "shell": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn shell",
        "cwd": "functions"
      }
    }
  },
  "tags": ["type:app", "platform:firebase", "scope:backend"],
  "implicitDependencies": ["shared-types"]
}
