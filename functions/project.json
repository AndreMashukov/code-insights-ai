{
  "name": "functions",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "functions/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn build",
        "cwd": "functions"
      },
      "dependsOn": ["^build"]
    },
    "build-with-deps": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run shared-types:build && yarn build",
        "cwd": "functions"
      },
      "dependsOn": ["shared-types:build"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn lint",
        "cwd": "functions"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run shared-types:build && cd functions && yarn build && cd .. && npx firebase emulators:start"
      },
      "dependsOn": ["shared-types:build"]
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run shared-types:build && yarn deploy",
        "cwd": "functions"
      },
      "dependsOn": ["shared-types:build"]
    },
    "shell": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn shell",
        "cwd": "functions"
      }
    }
  },
  "tags": ["type:app", "platform:firebase", "scope:backend"],
  "implicitDependencies": ["shared-types"]
}
