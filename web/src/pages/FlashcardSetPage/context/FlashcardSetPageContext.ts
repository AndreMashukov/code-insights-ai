import React from 'react';
import { IFlashcardSetPageContext } from '../types/IFlashcardSetPageContext';

export const FlashcardSetPageContext = React.createContext<IFlashcardSetPageContext | undefined>(undefined);
