// Effects hook is no longer needed since React Hook Form handles form state
// URL parameter handling is done in useCreateQuizPageForm hook
export const useCreateQuizPageEffects = () => {
  // This hook is kept for future non-form effects if needed
  // All form-related effects are handled by React Hook Form
};